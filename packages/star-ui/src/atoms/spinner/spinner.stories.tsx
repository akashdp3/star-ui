import React from 'react';

import Spinner from './Spinner';

export default {
  title: 'Spinner',
  component: Spinner
};

/**
 * TODO:
 * Stories:
 * [ ] Default spinner
 * [ ] Spinner with different sizes
 * [ ] Spinner with custom colour
 */

export const Default = () => {
  return <Spinner />;
};
